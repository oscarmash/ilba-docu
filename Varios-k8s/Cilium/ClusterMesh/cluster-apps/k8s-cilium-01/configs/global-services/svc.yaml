apiVersion: v1
kind: Service
metadata:
  name: global-service
  namespace: default
  annotations:
    io.cilium/global-service: "true"
spec:
  type: ClusterIP
  ports:
    - port: 80
  selector:
    name: deploy-cluster-02